/** Qualité Next/Image — plus nette que la valeur par défaut (75). */
export const IMAGE_DISPLAY_QUALITY = 85;

export const IMAGE_FIT_CONTAIN = "object-contain";
export const IMAGE_FIT_COVER = "object-cover";

/** Fond neutre pour les zones « letterbox » autour d'une image entière. */
export const IMAGE_FRAME_BG =
  "bg-[color:var(--color-surface-muted)]";
